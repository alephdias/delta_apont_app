<Window x:Class="DeltaApp.Desktop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delta Apont — Entrar" Height="480" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource Bg}">
    <Grid>
        <Border Style="{StaticResource Card}" Width="330" VerticalAlignment="Center">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <Border Background="{StaticResource Accent}" CornerRadius="8" Width="34" Height="34">
                        <TextBlock Text="Δ" Foreground="White" FontSize="18" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Delta Apont" FontSize="20" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="10,0,0,0"
                               Foreground="{StaticResource Text}"/>
                </StackPanel>
                <TextBlock Text="Entre com sua conta da empresa." Foreground="{StaticResource Muted}"
                           Margin="0,0,0,16"/>

                <TextBlock Text="E-mail" Style="{StaticResource Label}"/>
                <TextBox x:Name="EmailBox" Margin="0,0,0,12"/>

                <TextBlock Text="Senha" Style="{StaticResource Label}"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,8"/>

                <TextBlock x:Name="ErrorText" Foreground="#DC2626" TextWrapping="Wrap"
                           Visibility="Collapsed" Margin="0,4,0,4"/>

                <Button x:Name="LoginButton" Content="Entrar" Style="{StaticResource PrimaryButton}"
                        Margin="0,10,0,0" Click="LoginButton_Click" IsDefault="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
