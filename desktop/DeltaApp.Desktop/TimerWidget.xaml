<Window x:Class="DeltaApp.Desktop.TimerWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cronômetro" Width="248" Height="150"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowInTaskbar="False"
        WindowStartupLocation="Manual">
    <Border Background="#1F2430" CornerRadius="12" Padding="14" MouseLeftButtonDown="Drag">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.35"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="False">
                <Ellipse x:Name="StatusDot" Width="9" Height="9" Fill="#22C55E"
                         VerticalAlignment="Center" DockPanel.Dock="Left"/>
                <TextBlock x:Name="CodeText" Text="—" Foreground="White" FontWeight="SemiBold"
                           FontSize="14" Margin="8,0,0,0" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                <Button Content="✕" DockPanel.Dock="Right" Width="22" Height="22" Padding="0"
                        Background="Transparent" Foreground="#9CA3AF" BorderThickness="0"
                        Cursor="Hand" Click="Close_Click"/>
            </DockPanel>

            <TextBlock x:Name="TimeText" Grid.Row="1" Text="00:00:00" Foreground="White"
                       FontSize="30" FontWeight="Bold" Margin="0,6,0,8"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                <Button x:Name="PauseBtn" Content="⏸" Width="56" Height="30" Margin="0,0,6,0"
                        Cursor="Hand" Click="Pause_Click" ToolTip="Pausar"/>
                <Button x:Name="ContinueBtn" Content="▶" Width="56" Height="30" Margin="0,0,6,0"
                        Cursor="Hand" Click="Continue_Click" ToolTip="Continuar"/>
                <Button x:Name="FinishBtn" Content="■ Fim" Width="72" Height="30"
                        Cursor="Hand" Click="Finish_Click" ToolTip="Finalizar"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
